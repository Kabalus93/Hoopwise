import SwiftUI

// MARK: - Hub View (Recreated - Legacy)
/// This is a legacy view - FlightyHubView is the primary implementation
struct HubView: View {
    @EnvironmentObject var dataManager: DataManager
    
    var body: some View {
        FlightyHubView()
    }
}
