import SwiftUI

// MARK: - Flighty Passport Dashboard (Recreated - Legacy)
/// This is a legacy view - FlightyActionableDashboard is the primary implementation
struct FlightyPassportDashboard: View {
    @EnvironmentObject var dataManager: DataManager
    
    var body: some View {
        FlightyActionableDashboard()
    }
}
