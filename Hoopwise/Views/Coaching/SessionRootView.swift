import SwiftUI

// MARK: - Session Root View (Recreated - Legacy)
/// This is a legacy view - FlightySessionRootView is the primary implementation
struct SessionRootView: View {
    @EnvironmentObject var dataManager: DataManager
    
    var body: some View {
        FlightySessionRootView()
    }
}
